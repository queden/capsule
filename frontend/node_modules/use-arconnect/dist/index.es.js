import { useState, useEffect } from 'react';

var useArConnect = function () {
    var _a = useState(), arweaveWallet = _a[0], setArweaveWallet = _a[1];
    useEffect(function () {
        var loadWallet = function () { return setArweaveWallet(window.arweaveWallet); };
        if (window.arweaveWallet) {
            loadWallet();
        }
        else {
            addEventListener("arweaveWalletLoaded", loadWallet);
        }
    });
    return arweaveWallet;
};

export default useArConnect;
//# sourceMappingURL=index.es.js.map
