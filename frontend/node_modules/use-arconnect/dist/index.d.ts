declare const useArConnect: () => {
    connect(permissions: import("./declarations").PermissionType[]): Promise<void>;
    disconnect(): Promise<void>;
    getActiveAddress(): Promise<string>;
    getAllAddresses(): Promise<string[]>;
    getWalletNames(): Promise<{
        [addr: string]: string;
    }>;
    sign(transaction: any, options?: any): Promise<any>;
    getPermissions(): Promise<import("./declarations").PermissionType[]>;
    encrypt(data: string, options: {
        algorithm: string;
        hash: string;
        salt?: string;
    }): Promise<Uint8Array>;
    decrypt(data: Uint8Array, options: {
        algorithm: string;
        hash: string;
        salt?: string;
    }): Promise<string>;
};
export default useArConnect;
